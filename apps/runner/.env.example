PORT=5000
NODE_ENV=development

# Must match the API's RUNNER_SHARED_SECRET
RUNNER_SHARED_SECRET=change-me-to-a-long-random-string

# API callback URL (used by runner to report results back)
API_CALLBACK_URL=http://localhost:4000/internal/runner/result

# Optional
# SENTRY_DSN=